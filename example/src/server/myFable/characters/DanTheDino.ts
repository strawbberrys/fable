import { Character } from "@rbxts/fable";

export const DanTheDino = new Character("Dan the Dino");

DanTheDino.addDialogue("intro", ["What's up, I'm Dan.", "I eat everything around here."]);
DanTheDino.addDialogue("exit", ["Catch ya later.", "Nice seeing new names around here."]);
